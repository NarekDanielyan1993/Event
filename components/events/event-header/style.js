import { styled } from '@mui/system';

export const StyledEventHeader = styled('div')(() => ({
    '&': {
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
    },
}));
