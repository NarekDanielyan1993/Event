import StyledEventListContainer from './style';
const EventListContainer = ({ children }) => {
    return <StyledEventListContainer>{children}</StyledEventListContainer>;
};

export default EventListContainer;
