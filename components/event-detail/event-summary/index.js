import StyledLogisticSummary from './style';

function EventSummary({ title }) {
    return (
        <StyledLogisticSummary>
            <h1>{title}</h1>
        </StyledLogisticSummary>
    );
}

export default EventSummary;
